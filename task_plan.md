# Task Plan

## Goal
Remove the confirm password field from the sign-up form without breaking functionality, update tests via TDD, maintain accessibility and security.

## Phases
1. Setup & discovery (complete)
2. TDD: add failing tests for signup form changes (complete)
3. Implement minimal code changes (complete)
4. Refactor & cleanup (complete)
5. Verification (tests, coverage) (complete)

## Decisions
- Use existing Next.js architecture and Lumia-DS components when available.
- Update tests first per TDD skill.

## Errors Encountered
| Error | Attempt | Resolution |
| --- | --- | --- |
| zsh parse error near `${CLAUDE_PLUGIN_ROOT...` when running session-catchup | 1 | Will rerun with explicit path once available or proceed without catchup if not configured. |

